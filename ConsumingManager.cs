/*Thank you Randall, A lot of this code came from studying you. */

using Angder.EchoesOfTheFuture.Cards;
using FMOD.Studio;
using HarmonyLib;
using System;

namespace Angder.EchoesOfTheFuture;



public sealed class ConsumingManager
{
    public ConsumingManager()
    {
    }
    /*
    private static ModEntry Instance => ModEntry.Instance;
    

    public static bool IsConsuming(Card card, State state)
        => Instance.Helper.Content.Cards.IsCardTraitActive(state, card, Instance.Consuming);

    public static void SetConsuming(Card card, State state, bool value)
        => Instance.Helper.Content.Cards.SetCardTraitOverride(state, card, Instance.RemoteControl, value, permanent: false);

    public static void SetConsumingUnending(Card card, State state, bool value)
    => Instance.Helper.Content.Cards.SetCardTraitOverride(state, card, Instance.RemoteControl, value, permanent: true);
    */ /*
    public static bool RemoteOverride(Card Test, State s)
    {
        if (IsRemoteControlled(Test, s))
        {
            return true;
        }
        return false;
    }
    */



}
